---
title: Quickstart
description: Minimal steps to register an application, authenticate, create paytags, and route transactions through the switch.
---

# Quickstart

Use this path when you want to get a switching integration working quickly in staging.

## 1. Register your application

```bash
curl -X POST https://staging-switch.yourflexpay.com/api/v1/applications \
  -H 'Content-Type: application/json' \
  -d '{
    "name": "WalletApp",
    "appHandle": "walletapp",
    "transactionWebhookUrl": "https://wallet-app.example.com/switch/webhook"
  }'
```

Save these values immediately from the response:

- `clientId`
- `clientSecret`
- `webhookSecret`
- `appPrivateKeyWrapped` if you plan to use JWE

## 2. Exchange credentials for a token

```bash
curl -X POST https://staging-switch.yourflexpay.com/api/v1/auth/token \
  -H 'Content-Type: application/json' \
  -d '{
    "client_id": "<clientId>",
    "client_secret": "<clientSecret>"
  }'
```

Store the `token` value and send it as:

```text
Authorization: Bearer <token>
```

## 3. Create a subject

```bash
curl -X POST https://staging-switch.yourflexpay.com/api/v1/subjects \
  -H 'Authorization: Bearer <token>' \
  -H 'Content-Type: application/json' \
  -d '{
    "externalId": "user-123",
    "displayName": "Alice Doe"
  }'
```

## 4. Create a paytag

```bash
curl -X POST https://staging-switch.yourflexpay.com/api/v1/tags \
  -H 'Authorization: Bearer <token>' \
  -H 'Content-Type: application/json' \
  -d '{
    "subjectId": "<subject-id>",
    "tagString": "alice"
  }'
```

The globally unique paytag becomes `alice@walletapp`.

## 5. Route a transaction

```bash
curl -X POST https://staging-switch.yourflexpay.com/api/v1/transactions \
  -H 'Authorization: Bearer <token>' \
  -H 'Content-Type: application/json' \
  -H 'Idempotency-Key: txn-001' \
  -d '{
    "senderTag": "alice@walletapp",
    "receiverTag": "bob@shopapp",
    "amount": 500,
    "currency": "NGN",
    "callbackUrl": "https://wallet-app.example.com/switch/callback"
  }'
```

## 6. Handle the receiver webhook

The switch sends a `TRANSACTION_INITIATED` webhook to the receiving application. The receiver must:

1. Verify `X-Switch-Signature`
2. Optionally decrypt the payload if encryption is enabled
3. Accept or reject the transaction through the API

Continue with [Webhooks](/switch/webhooks) and [Payments](/switch/payments) for the full flow.

